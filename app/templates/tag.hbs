<div class="row">
  <div class="col text-right">
    {{#bs-button type="primary" onClick=(action "openModal")}}Add Tag+{{/bs-button}}
  </div>
</div>

<div class="row">
  <div class="col">
    <table class="table table-sm table-bordered">
      <tr>
        <td>Tag</td>
      </tr>
      {{#each model as |tag|}}
        <tr>
          <td>
            {{tag.name}}
          </td>
        </tr>
      {{/each}}
    </table>
  </div>
</div>

{{#bs-modal open=isModalOpen onHidden=(action "modalClosed")}}
  <div class="modal-body">
    <form {{action "addTag" on="submit"}}>
      <label>Name: </label>
      {{input value=name class="form-control"}}
      <button type="submit" class="btn btn-primary">Add Tag</button>
    </form>
  </div>
{{/bs-modal}}

{{outlet}}
